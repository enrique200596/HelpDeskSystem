@using Microsoft.AspNetCore.Components.Web

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <link rel="stylesheet" href="bootstrap/bootstrap.min.css" />
    <link rel="stylesheet" href="app.css" />
    <link rel="stylesheet" href="HelpDeskSystem.Web.styles.css" />
    <link rel="icon" type="image/png" href="favicon.png" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet" />

    @* --- LIBRERÍAS EXTERNAS (CARGA PRIORITARIA) --- *@
    @* Quill CSS *@
    <link href="//cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
    <link href="//cdn.quilljs.com/1.3.6/quill.bubble.css" rel="stylesheet">

    @* Quill JS: Movido al head para solucionar el error "Quill is not defined" *@
    <script src="_content/Blazored.TextEditor/quill.min.js"></script>
    <script src="_content/Blazored.TextEditor/Blazored-BlazorQuill.js"></script>

    @* --- RENDERIZADO INTERACTIVO: prerender: false es vital para JS Interop --- *@
    <HeadOutlet @rendermode="new InteractiveServerRenderMode(prerender: false)" />
</head>
<body>
    <Routes @rendermode="new InteractiveServerRenderMode(prerender: false)" />

    @* --- SCRIPTS DE FRAMEWORK Y UI --- *@
    <script src="_framework/blazor.web.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    @* --- SCRIPTS PERSONALIZADOS --- *@
    <script src="js/notificaciones.js"></script>
    <script>
        // Función para descarga de archivos (Reportes)
        window.descargarArchivo = (nombre, base64) => {
            const link = document.createElement('a');
            link.download = nombre;
            link.href = "data:application/octet-stream;base64," + base64;
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);
        }
    </script>
</body>
</html>